name: Keep Me Alive

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  keep-active:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Make a commit to mark activity
      run: |
        date >> activity-log.txt
        git config --global user.name 'Sujoy De'
        git config --global user.email 'sujoy.de345@gmail.com'
        git add activity-log.txt
        git commit -m "Marking profile active"

    - name: Push changes
      run: git push
